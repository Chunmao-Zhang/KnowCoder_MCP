# KnowCoder MCP 设计摘要

## 长任务

MCP 工具快速创建后台任务并返回令牌。宿主使用一个串行长轮询读取变化。每次等待结束后，宿主根据返回状态决定继续等待、展示审阅或读取结果。

## 用户确认

问题拆解和 Schema 完成后任务进入 `waiting`。MCP 返回完整内容和保存到 Workspace `review/` 目录的只读 HTML 页面。页面不依赖 MCP 进程，重启后仍可查看。用户在原来的 Agent 对话中确认或提出自然语言修改。未收到用户决定时任务不继续运行。

## 状态与隔离

每轮运行有独立 `task_id`，长期内容使用稳定的 `workspace_id`。Subagent 由脚本绑定到当前 Session、Attempt 和相对虚拟目录。状态、租约、候选文件和正式 Workspace 分开保存。

## 恢复与停止

宿主重启后继续使用 `continuation_token`。令牌丢失时使用任务列表让用户选择。外部 API 重试耗尽后任务进入 `failed`，修复配置后使用原令牌恢复。`stop_task` 只停止活跃 Worker。

## 增量调研

增量请求继续写入原 Workspace。问题计划保留已确认内容并增加缺口；Schema 可表达时复用，无法表达时增加必要结构；抽取只处理新增或变化来源。最终文档描述累计范围。

## 文件发布

Subagent 写候选文件。Validator 校验后脚本接受阶段结果。完整 Workspace 先写入独立快照，再发布当前版本。失败的候选不会替换上一份可用 Workspace。

## 公共接口

普通宿主只看到六个工具：开始、等待、提交审阅决定、读取 Workspace、查找任务、停止任务。安装配置只要求主调研模型、抽取模型和 Serper 三组信息。
